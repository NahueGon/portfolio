<!doctype html>
<html lang="es" class="dark">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={ Astro.generator } />
		<title>Portafolio de Nahuel Gonzalez - Desarrollador y Programador</title>
	</head>
	<body class="relative text-black dark:text-white">
		<div
			class="fixed top-0 bottom-0 z-[-2] h-full w-full bg-gray-100 dark:bg-gray-950
			bg-[radial-gradient(ellipse_100%_100%_at_0%_10%,rgba(100,110,220,0.2),rgba(255,255,255,0)),radial-gradient(ellipse_90%_100%_at_90%_100%,rgba(215,104,1,0.1),rgba(255,255,255,0))] 
			dark:bg-[radial-gradient(ellipse_100%_100%_at_0%_10%,rgba(100,110,90,0.1),rgba(0,0,0,0.2)),radial-gradient(ellipse_90%_100%_at_90%_100%,rgba(215,104,1,0.1),rgba(255,255,255,0))]">
		</div>
		<slot />
	</body>


</html>

<style is:global>
	:root {
		color-scheme: light dark;
	}

	html {
		font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
	}

	body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        overscroll-behavior: none;
	}
	
	@media (prefers-reduced-motion: reduce) {
        html {
          	scroll-behavior: auto;
        }
	}

	@keyframes fadeIn {
		0% {
			opacity: 0;
		}
		100% {
			opacity: 1; 
		}
	}

	.fade-in {
		animation: fadeIn 1s ease-out;
	}

</style>